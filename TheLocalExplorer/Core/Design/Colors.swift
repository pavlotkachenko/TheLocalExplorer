import SwiftUI

enum AppColors {
    static let tomatoRed = Color(hex: "#D9381E")
    static let oatmeal = Color(hex: "#F4F1EA")
    static let pureWhite = Color.white
    static let espresso = Color(hex: "#2C2A26")
    static let mushroom = Color(hex: "#8A857D")
    static let mustard = Color(hex: "#C18C1D")
    static let oliveGreen = Color(hex: "#556B2F")
    static let skeleton = Color(hex: "#EAE6DD")

    static let cardShadow = Color.black.opacity(0.08)
    static let softShadow = Color.black.opacity(0.05)
}
